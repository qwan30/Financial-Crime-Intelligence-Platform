# Current Code and Git History Review Plan

## Context
The repository is on clean `main` at `39fe39d619bb7f4d1eb8a391b5b2b4860380988c`, synchronized with `origin/main`; the fixed baseline is `05f02c4e18165f7f6f4ff011351b1d8b089747fb`. The requested review must independently assess the 116-file integrated change (`+18,969/-94`), diagnose observable failures rather than speculate, and produce a severity-ranked readiness verdict. The parent session is the GPT-5.6-Sol supervisor; fresh specialist agents provide independent review seats.

## Approach
1. Freeze the read-only review range as `05f02c4e18165f7f6f4ff011351b1d8b089747fb..39fe39d619bb7f4d1eb8a391b5b2b4860380988c` and separately inspect the empty working-tree/index diffs, first-parent merge history, full commit list, changed-file inventory, and diff statistics. Confirm the nine dependency-ordered integrations—CI, evidence/cases, agent, API, UI, streaming, MLOps/monitoring, release, then documentation—and treat `reports/2026-09-02-pre-pr-review-summary.md` as stale pre-split evidence rather than current proof.
2. Dispatch independent read-only agents in one parallel wave: a `code-reviewer` using the requesting-code-review rubric for requirements, architecture, correctness, security, and production readiness; a `silent-failure-hunter` for swallowed errors and false-success paths; a `pr-test-analyzer` for behavioral coverage and false-confidence tests; and a `scout` for history coherence and change-to-test mapping. Give every seat the fixed full SHAs, its exclusive surface, no-subagent/no-edit constraints, and instructions to skip validation commands. If quota prevents a specialized seat from starting, immediately replace only that seat with a tightly scoped read-only `scout`; do not reduce the review surface.
3. Review against `docs/superpowers/plans/2026-08-30-investigator-deepseek.md`, `docs/superpowers/plans/2026-09-02-mlops-streaming-release.md`, and `.orchestration/tasks/TASK-003-investigator-deepseek.md` / `TASK-004-mlops-streaming-release.md`. Treat these code-review hypotheses as mandatory falsification targets, not accepted findings: unauthenticated case/workbench/feedback access; process-local loss of cases, evidence, and feedback; client-controlled feedback snapshot hashes; paid LLM generation from retryable `GET /workbench`; frozen-evaluation conflicts swallowed in `src/fincrime/agent/evaluation.py`; declared-but-unwired Prometheus metrics; mutable action/tool references in scheduled security CI; unchecked frontend HTTP/JSON casts; mock-only Playwright coverage; and CI’s omission of Playwright plus narrower MyPy scope.
4. Build the diagnostic feedback loop from the repository's declared verification commands. Run the smallest targeted test or temporary one-shot harness identified by each Critical/Important finding first; require it to drive the exact reachable path and return the same result twice. Then run the complete Python and frontend verification matrix for regression evidence. Redact secrets and never invoke the real DeepSeek endpoint; use the existing guarded provider test transports.
5. Have the GPT-5.6-Sol supervisor deduplicate and reconcile findings against the fixed diff, requirements, caller paths, and command output. Reject unsupported speculation; retain Critical and Important issues only with exact file/line, reachable trigger, impact, and concrete remediation, and record Minor issues separately. No code changes are made during this review; subagent-driven development is reserved for a separately approved remediation plan because the literal scope is status review.
6. Produce a final status report containing repository state, exact reviewed range, nine-merge chronology, verification commands and outcomes, inspected/unreviewed surfaces, strengths, severity-ranked findings, recommendations, and an explicit `Yes`, `No`, or `With fixes` readiness verdict. Correct the stale report comparison: its `110 files, +15,583/-147` and monolithic pre-PR packaging state do not describe current `main`.

## Critical files & anchors
- `reports/2026-09-02-pre-pr-review-summary.md:1-108` — prior 572-test claim and `05f02c4` baseline, but stale branch/diff/packaging status.
- `.orchestration/supervisor-notebook.md:96-116` — implementation waves, independent reviews, quarantine-store race fix, and certification claims to verify.
- `apps/case_api/main.py:125` and route handlers below it — composite workbench contract that lacks a direct API integration test.
- `apps/investigator-web/src/api.ts:130-208` plus `apps/investigator-web/e2e/workbench.spec.ts:116-187` — unchecked HTTP/JSON casting and fully mocked browser network boundary.
- `.github/workflows/ci.yml:28-99` — CI runs MyPy only on `src`, runs frontend unit/build lanes, and does not run Playwright.

## Verification
- Working directory: repository root. Require `rtk` on `PATH`, Python 3.12 with the existing `uv.lock`, and the existing frontend installation under `apps/investigator-web`; use the checked-in `package-lock.json` as the dependency contract. Do not install or update dependencies during this read-only review.
- Repository freeze: `rtk git rev-parse HEAD origin/main 05f02c4e18165f7f6f4ff011351b1d8b089747fb`, `rtk git status --short --branch`, `rtk git diff --exit-code`, and `rtk git diff --cached --exit-code`. Expected: HEAD and `origin/main` are `39fe39d619bb7f4d1eb8a391b5b2b4860380988c`, status contains only `## main...origin/main`, and both diffs exit 0.
- History evidence: `rtk git merge-base 05f02c4e18165f7f6f4ff011351b1d8b089747fb 39fe39d619bb7f4d1eb8a391b5b2b4860380988c`, `rtk git log --graph --oneline --first-parent 05f02c4e18165f7f6f4ff011351b1d8b089747fb..39fe39d619bb7f4d1eb8a391b5b2b4860380988c`, `rtk git diff --stat 05f02c4e18165f7f6f4ff011351b1d8b089747fb..39fe39d619bb7f4d1eb8a391b5b2b4860380988c`, and `rtk git diff-tree --no-commit-id --name-only -r 39fe39d619bb7f4d1eb8a391b5b2b4860380988c`. Expected: merge base equals `05f02c4...`, nine first-parent integrations, `116 files changed, 18969 insertions(+), 94 deletions(-)`, and the head commit changes documentation/orchestration artifacts only.
- Python gates from repository root: `rtk test uv run --all-groups pytest -q --tb=short`, `rtk uv run ruff format --check src tests`, `rtk uv run ruff check .`, `rtk uv run mypy src apps`, and `rtk uv build`. Expected: exit 0 for each; note that `mypy src apps` is deliberately stricter than current CI’s `mypy src`.
- Frontend gates from `apps/investigator-web`: `rtk npm run test -- --run`, `rtk npm run build`, and `rtk npx playwright test`. Expected: exit 0; record that Playwright intercepts both workbench and feedback requests and therefore verifies UI behavior, not live FastAPI integration.
- Finding-specific proof must exercise each candidate twice with identical results:
  - Seed the FastAPI dependencies and call case, workbench, and feedback routes without credentials; any protected case data or accepted adjudication confirms the authentication/authorization blocker.
  - Create state in one application instance, construct a fresh instance, and read the same case; `404` after restart confirms process-local persistence. Construct two instances over independent default stores to demonstrate worker divergence.
  - Submit feedback with a valid-looking 64-character hash different from `CaseSnapshot.snapshot_hash`; a success envelope and stored mismatched hash confirm broken lineage.
  - Inject a counting `GuardedDeepSeekProvider`, call `GET /cases/{case_id}/workbench` twice for the same immutable snapshot, and compare call count/hypothesis IDs; two provider calls confirm non-idempotent paid work on a cacheable read.
  - Pre-seed the evaluation repositories with same-ID/different-bytes gold evidence and case records, then invoke the frozen-corpus evaluator; continuing instead of raising an integrity error confirms manifest-to-runtime drift.
  - Capture Prometheus counter/histogram samples before and after one accepted replay, one quarantined replay, and one scoring call, then request the application scrape route; unchanged samples or no scrape route confirm disconnected monitoring.
  - Inspect every third-party `uses:` value and every `uv`/`uvx` security-tool version in `.github/workflows/security.yml`; any mutable tag, `latest`, or unpinned tool confirms supply-chain exposure. Verify `security-events: write` is scoped only to a job that needs it.
  - Stub frontend fetch with non-2xx success-shaped JSON and structurally invalid JSON; accepting either as a typed success confirms absent status/schema validation.
  - Run the existing Playwright journey and capture its network log; only fulfilled route mocks and no FastAPI request confirm the UI-only seam. Compare `.github/workflows/ci.yml` against local commands to confirm absent Playwright and `apps` MyPy lanes.

## Assumptions & contingencies
- Scope is the integrated work from `05f02c4e18165f7f6f4ff011351b1d8b089747fb` through current `main` at `39fe39d619bb7f4d1eb8a391b5b2b4860380988c`; current working-tree changes are empty. If HEAD moves before execution, set `HEAD_SHA` once from `rtk git rev-parse HEAD`, report the movement, and substitute that captured immutable SHA for `39fe39d619bb7f4d1eb8a391b5b2b4860380988c` throughout the review without recapturing it.
- `rtk` is currently absent from the shell PATH, matching `.orchestration/supervisor-notebook.md:40-81`. Do not bypass the repository command policy or install software: complete source/history inspection through read-only tools, mark terminal-based behavioral gates blocked, and make the final readiness verdict `With fixes` or `No` because runtime evidence is incomplete.
- If a lane additionally requires unavailable credentials or services, run every unblocked local lane, mark only the affected lane unverified, and require a redacted captured artifact or accessible environment before making claims about it. The DeepSeek lane remains mocked regardless of credential availability to avoid external cost and sensitive-data side effects.
